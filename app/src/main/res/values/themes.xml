<resources>
    <style name="Theme.MyApp" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Status bar uses @color/app_window_bg (= [AppColors.Bg] #F3EFE4); windowLightStatusBar → dark icons. -->
        <item name="android:statusBarColor">@color/app_window_bg</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowBackground">@color/app_window_bg</item>
    </style>

    <style name="Theme.MyApp.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/app_window_bg</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="postSplashScreenTheme">@style/Theme.MyApp</item>
        <item name="android:windowBackground">@color/app_window_bg</item>
        <item name="android:statusBarColor">@color/app_window_bg</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>
</resources>
